import styled from "@emotion/styled";

const GlobalWidth = styled.div`
  position: relative;
  max-width: 640px;
  min-width: 320px;
  overflow-x: hidden;
  margin: 0 auto;
`;

export default GlobalWidth;
